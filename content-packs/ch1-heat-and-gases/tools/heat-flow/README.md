# Heat Flow Lab

Interactive heat-flow and thermal-equilibrium lab. Edit files in this folder; `npm run build` syncs them to `public/heat-flow/` and `dist/`.

Open standalone: `index.html` (use `?lang=zh|en` and `?embed=1` when embedded in the hub).
