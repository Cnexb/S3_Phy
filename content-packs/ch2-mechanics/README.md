# CH2 Force and Motion chapter pack (side branch)

**Do not merge this branch into `main`.** Hub `labs/`, `quizzes/`, and `public/` stay for vendor Pages.

No chapter quiz JSON exists in `quizzes/` yet, so `quiz/` is empty.

```
content-packs/ch2-mechanics/
  manifest.json
  notes/
  comics/
  tools/          vector-tool, newton, moments, momentum, projectile, orbital, shared/
  quiz/
  summaries/
  flashcards/     mechanics JSON
```

```bash
node scripts/test-ch2-content-pack.mjs
node scripts/test-chapter-assets.mjs
```
