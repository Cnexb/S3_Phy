# Boiling Water Energy Lab

Interactive heat / energy lab for heating water. Edit files in this folder; `npm run build` syncs them to `public/boiling-water/` and `dist/`.

Open standalone: `index.html` (use `?lang=zh|en` and `?embed=1` when embedded in the hub).
