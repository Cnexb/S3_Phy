# Boyle's Law Lab

Interactive lab. Edit files in this folder; `npm run build` syncs them to `public/boyles-law/`.

Open standalone: `index.html`. Use `?lang=zh|en` and `?embed=1` when embedded in the hub.
